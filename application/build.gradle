apply plugin: 'org.springframework.boot'

jar {
    baseName = 'gfc-services'
    version = '1.0.0'
}

dependencies {
	// TODO revue de code car pas certain de l'interet ici
	api( project( ':compta-budgetaire:api' ) )
	implementation( project( ':compta-budgetaire:implementation' ) )
	api( project( ':compta-generale:api' ) )
	implementation( project( ':compta-generale:implementation' ) )

    compile('org.springframework.boot:spring-boot-starter-actuator')
    compile('org.springframework.boot:spring-boot-starter-web')
    
    testCompile('org.springframework.boot:spring-boot-starter-test')
}
